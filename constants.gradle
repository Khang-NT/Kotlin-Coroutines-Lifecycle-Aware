project.ext {
    config = [compileSdkVersion: 27,
              buildToolsVersion: "27.0.3",
              targetSdkVersion : 27,
              minSdkVersion    : 16]

    kotlinCoroutinesVersion = "0.22.5"
    lifecycleVersion = "1.1.1"
}